node_modules/
dist/
*.zip
.env
.env.local
# Per-deploy env overrides for scripts/deploy.sh — machine-local, never committed.
deploy.env
# Extra Lockbox bindings for scripts/deploy.sh (ENV_VAR=secretId:key) — machine-local too.
deploy.secrets
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
*.tfvars
*.auto.tfvars
*.log
